# src/ui/ — frontend surfaces

Policy (docs/REPO_STANDARD.md §2): components, views, and screens live here.
This repo ships a headless kernel, so the folder is reserved. First UI surface
moves this repo's class to `fullstack` in seeds/seeds.yaml + package.json.
